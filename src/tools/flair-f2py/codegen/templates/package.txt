! Generated by flair-f2py -- do not edit.
module py_{pkg}_pkg_mod
    use python_api_mod
    use iso_c_binding
    implicit none

    ! ===== Persistent C strings (null-terminated; target+save keeps c_loc valid) =====
{cstrings}
    ! ===== Persistent tables (target+save keeps c_loc valid for the module lifetime) =====
    type(PyMethodDef_t), target, save :: pkg_methods(1)
    type(PyModuleDef_t), target, save :: {pkg}_pkg_moddef

    ! ===== Internal inits of the wrapped modules (same .so, matched by name at link time) =====
    interface
{init_ifaces}    end interface
contains

    function PyInit_{pkg}() bind(C, name="PyInit_{pkg}") result(r)
        type(c_ptr) :: r, pkg_ptr, sub, modules_dict
        integer(c_int) :: rc

        r = c_null_ptr
        ! --- package method table ---
{method_fills}        ! --- module def ---
{moddef_fills}
        pkg_ptr = PyModule_Create2(c_loc({pkg}_pkg_moddef), PYTHON_ABI_VERSION)
        if (.not. c_associated(pkg_ptr)) return
        modules_dict = PyImport_GetModuleDict()   ! borrowed reference

        ! Each flair_init_* returns a new reference; PyModule_AddObjectRef and
        ! PyDict_SetItemString take their own, so the creation reference is
        ! dropped exactly once. The sys.modules entry "{pkg}.<mod>" is what
        ! makes `import {pkg}.<mod>` and `from {pkg}.<mod> import X` work.
{submodules}        r = pkg_ptr
    end function
end module py_{pkg}_pkg_mod
